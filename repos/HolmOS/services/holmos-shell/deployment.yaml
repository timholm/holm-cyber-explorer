---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: holmos-shell
  namespace: holm
  labels:
    app: holmos-shell
    holmos.io/component: shell
spec:
  replicas: 1
  selector:
    matchLabels:
      app: holmos-shell
  template:
    metadata:
      labels:
        app: holmos-shell
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: kubernetes.io/hostname
                operator: NotIn
                values:
                - openmediavault
      containers:
      - name: holmos-shell
        image: registry.holm.svc.cluster.local:5000/holmos-shell:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
          name: http
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "256Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: holmos-shell
  namespace: holm
  labels:
    app: holmos-shell
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 8080
    nodePort: 30000
    protocol: TCP
    name: http
  selector:
    app: holmos-shell
