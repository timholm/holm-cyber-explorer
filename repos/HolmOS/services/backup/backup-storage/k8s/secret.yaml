apiVersion: v1
kind: Secret
metadata:
  name: backup-storage-db-secret
  namespace: holm
type: Opaque
stringData:
  # WARNING: Set real values in cluster, not here!
  # Use: kubectl create secret generic backup-storage-db-secret --from-literal=username=VALUE --from-literal=password=VALUE -n holm
  username: "CHANGE_ME_IN_CLUSTER"
  password: "CHANGE_ME_IN_CLUSTER"
