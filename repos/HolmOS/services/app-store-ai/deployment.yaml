---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-store-ai
  namespace: holm
  labels:
    app: app-store-ai
    holmos.io/component: app-store
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-store-ai
  template:
    metadata:
      labels:
        app: app-store-ai
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: kubernetes.io/hostname
                operator: NotIn
                values:
                - openmediavault
      containers:
      - name: app-store-ai
        image: registry.holm.svc.cluster.local:5000/app-store-ai:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
          name: http
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "256Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: app-store-ai
  namespace: holm
  labels:
    app: app-store-ai
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 8080
    nodePort: 30002
    protocol: TCP
    name: http
  selector:
    app: app-store-ai
