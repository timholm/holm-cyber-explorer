apiVersion: apps/v1
kind: Deployment
metadata:
  name: file-delete
  namespace: holm
spec:
  replicas: 1
  selector:
    matchLabels:
      app: file-delete
  template:
    metadata:
      labels:
        app: file-delete
    spec:
      nodeSelector:
        kubernetes.io/arch: arm64
      containers:
      - name: file-delete
        image: registry.holm.svc.cluster.local:5000/holm/file-delete:v1
        ports:
        - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: file-delete
  namespace: holm
spec:
  selector:
    app: file-delete
  ports:
  - port: 8080
    targetPort: 8080
