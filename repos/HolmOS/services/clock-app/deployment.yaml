---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: clock-app
  namespace: holm
  labels:
    app: clock-app
    holmos.io/component: clock
spec:
  replicas: 1
  selector:
    matchLabels:
      app: clock-app
  template:
    metadata:
      labels:
        app: clock-app
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: kubernetes.io/hostname
                operator: NotIn
                values:
                - openmediavault
      containers:
      - name: clock-app
        image: registry.holm.svc.cluster.local:5000/clock-app:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 30007
          name: http
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "256Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 30007
          initialDelaySeconds: 10
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /health
            port: 30007
          initialDelaySeconds: 5
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: clock-app
  namespace: holm
  labels:
    app: clock-app
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 30007
    nodePort: 30011
    protocol: TCP
    name: http
  selector:
    app: clock-app
