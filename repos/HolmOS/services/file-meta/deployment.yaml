apiVersion: apps/v1
kind: Deployment
metadata:
  name: file-meta
  namespace: holm
spec:
  replicas: 1
  selector:
    matchLabels:
      app: file-meta
  template:
    metadata:
      labels:
        app: file-meta
    spec:
      nodeSelector:
        kubernetes.io/arch: arm64
      containers:
      - name: file-meta
        image: registry.holm.svc.cluster.local:5000/holm/file-meta:v1
        ports:
        - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: file-meta
  namespace: holm
spec:
  selector:
    app: file-meta
  ports:
  - port: 8080
    targetPort: 8080
