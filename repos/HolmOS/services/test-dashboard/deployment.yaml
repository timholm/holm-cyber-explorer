---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: test-dashboard
  namespace: holm
  labels:
    app: test-dashboard
    holmos.io/component: test
spec:
  replicas: 1
  selector:
    matchLabels:
      app: test-dashboard
  template:
    metadata:
      labels:
        app: test-dashboard
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: kubernetes.io/hostname
                operator: NotIn
                values:
                - openmediavault
      containers:
      - name: test-dashboard
        image: registry.holm.svc.cluster.local:5000/test-dashboard:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
          name: http
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "256Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: test-dashboard
  namespace: holm
  labels:
    app: test-dashboard
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 8080
    nodePort: 30018
    protocol: TCP
    name: http
  selector:
    app: test-dashboard
