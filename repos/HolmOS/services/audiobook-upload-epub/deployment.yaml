---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: audiobook-upload-epub
  namespace: holm
  labels:
    app: audiobook-upload-epub
    holmos.io/component: audiobook
spec:
  replicas: 1
  selector:
    matchLabels:
      app: audiobook-upload-epub
  template:
    metadata:
      labels:
        app: audiobook-upload-epub
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: kubernetes.io/hostname
                operator: NotIn
                values:
                - openmediavault
      containers:
      - name: audiobook-upload-epub
        image: registry.holm.svc.cluster.local:5000/audiobook-upload-epub:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
          name: http
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "256Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: audiobook-upload-epub
  namespace: holm
  labels:
    app: audiobook-upload-epub
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 8080
    nodePort: 30019
    protocol: TCP
    name: http
  selector:
    app: audiobook-upload-epub
