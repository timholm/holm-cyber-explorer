apiVersion: apps/v1
kind: Deployment
metadata:
  name: file-upload
  namespace: holm
spec:
  replicas: 1
  selector:
    matchLabels:
      app: file-upload
  template:
    metadata:
      labels:
        app: file-upload
    spec:
      nodeSelector:
        kubernetes.io/arch: arm64
      containers:
      - name: file-upload
        image: registry.holm.svc.cluster.local:5000/holm/file-upload:v1
        ports:
        - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: file-upload
  namespace: holm
spec:
  selector:
    app: file-upload
  ports:
  - port: 8080
    targetPort: 8080
