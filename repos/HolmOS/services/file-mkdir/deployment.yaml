apiVersion: apps/v1
kind: Deployment
metadata:
  name: file-mkdir
  namespace: holm
spec:
  replicas: 1
  selector:
    matchLabels:
      app: file-mkdir
  template:
    metadata:
      labels:
        app: file-mkdir
    spec:
      nodeSelector:
        kubernetes.io/arch: arm64
      containers:
      - name: file-mkdir
        image: registry.holm.svc.cluster.local:5000/holm/file-mkdir:v1
        ports:
        - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: file-mkdir
  namespace: holm
spec:
  selector:
    app: file-mkdir
  ports:
  - port: 8080
    targetPort: 8080
