apiVersion: apps/v1
kind: Deployment
metadata:
  name: file-search
  namespace: holm
spec:
  replicas: 1
  selector:
    matchLabels:
      app: file-search
  template:
    metadata:
      labels:
        app: file-search
    spec:
      nodeSelector:
        kubernetes.io/arch: arm64
      containers:
      - name: file-search
        image: registry.holm.svc.cluster.local:5000/holm/file-search:v1
        ports:
        - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: file-search
  namespace: holm
spec:
  selector:
    app: file-search
  ports:
  - port: 8080
    targetPort: 8080
