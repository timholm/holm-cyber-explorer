---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: settings-web
  namespace: holm
  labels:
    app: settings-web
    holmos.io/component: settings
spec:
  replicas: 1
  selector:
    matchLabels:
      app: settings-web
  template:
    metadata:
      labels:
        app: settings-web
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: kubernetes.io/hostname
                operator: NotIn
                values:
                - openmediavault
      containers:
      - name: settings-web
        image: registry.holm.svc.cluster.local:5000/settings-web:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
          name: http
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "256Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: settings-web
  namespace: holm
  labels:
    app: settings-web
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 8080
    nodePort: 30600
    protocol: TCP
    name: http
  selector:
    app: settings-web
