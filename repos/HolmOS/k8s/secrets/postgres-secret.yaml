apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
  namespace: holm
type: Opaque
stringData:
  password: "CHANGE_ME_IN_CLUSTER"
  # Full connection string for services that need it
  database-url: "postgres://holm:CHANGE_ME_IN_CLUSTER@postgres.holm.svc.cluster.local:5432/holm?sslmode=disable"
