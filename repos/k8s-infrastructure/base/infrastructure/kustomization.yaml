apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace
  - network-policies
  - metallb
  - envoy-gateway
  - argo-rollouts
  # cloudflare-tunnel — disabled: local networking via MetalLB (TASK-029)
  - argo-workflows
  - velero
  - velero-ui
  - qnap-csi
