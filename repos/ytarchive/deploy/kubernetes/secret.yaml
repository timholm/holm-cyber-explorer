apiVersion: v1
kind: Secret
metadata:
  name: ytarchive-secrets
  namespace: ytarchive
  labels:
    app.kubernetes.io/name: secrets
    app.kubernetes.io/component: configuration
    app.kubernetes.io/part-of: ytarchive
type: Opaque
data:
  # Redis connection URL (base64 encoded: redis://redis-service:6379)
  redis-url: cmVkaXM6Ly9yZWRpcy1zZXJ2aWNlOjYzNzk=
stringData:
  # Webhook URL for notifications (e.g., Slack, Discord)
  WEBHOOK_URL: ""

  # YouTube Data API key (optional, for channel metadata)
  YOUTUBE_API_KEY: ""

  # Additional secret configuration
  # Add your secrets here before deploying
